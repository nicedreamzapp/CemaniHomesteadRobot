[env:teensy41]
platform = teensy
board = teensy41
framework = arduino
upload_protocol = teensy-cli
monitor_speed = 115200

; Enable USB Host for Xbox controller
build_flags = -D USB_SERIAL_HID

; FlasherX is now embedded in main.cpp - no external dependencies needed
; This allows standalone .ino files to be compiled on VPS without library
